#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../cfb/bin/cfb" "$@"
  ret=$?
else 
  node  "$basedir/../cfb/bin/cfb" "$@"
  ret=$?
fi
exit $ret
